apiVersion: v1
kind: Service
metadata:
  name: {{ include "hope-payment-gateway.fullname" . }}
  labels:
    {{- include "hope-payment-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hope-payment-gateway.selectorLabels" . | nindent 4 }}
